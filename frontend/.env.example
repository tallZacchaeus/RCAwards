# Base URL of the FastAPI backend. The marketing site reads categories and posts
# newsletter signups here. Defaults to http://localhost:8000 when unset.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Public site origin — used for robots.txt and sitemap.xml absolute URLs.
NEXT_PUBLIC_SITE_URL=https://awards.thecitybreed.org
